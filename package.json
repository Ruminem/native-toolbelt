{
  "name": "native-toolbelt",
  "displayName": "Native Toolbelt",
  "description": "Tools for native development on Windows: which DLLs an executable needs and which of them this machine cannot find, and which library defines the symbol a link error could not resolve.",
  "version": "0.2.0",
  "publisher": "Ruminem",
  "license": "Apache-2.0",
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/Ruminem/native-toolbelt.git"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "c++",
    "dll",
    "pe",
    "dependencies",
    "windows",
    "linker",
    "lnk2019"
  ],
  "engines": {
    "vscode": "^1.80.0"
  },
  "main": "./extension.js",
  "l10n": "./l10n",
  "activationEvents": [],
  "contributes": {
    "commands": [
      {
        "command": "nativeToolbelt.checkDlls",
        "title": "%command.checkDlls%"
      },
      {
        "command": "nativeToolbelt.decodeLinkError",
        "title": "%command.decodeLinkError%"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "nativeToolbelt.checkDlls",
          "when": "resourceExtname == .exe || resourceExtname == .dll",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "test": "node test.js",
    "package": "npx --yes @vscode/vsce@3 package"
  }
}
