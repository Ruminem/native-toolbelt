- 여기까지 됨: DLL 의존성 점검 기능. PE 파일을 직접 읽어 일반·지연 임포트를 뽑고 윈도우 검색 순서대로 찾음. 못 찾은 것이 바이너리 폴더 안에 있으면 그 경로를 알려 줌. notepad.exe(64·32비트)와 Code.exe 로 확인, vsix 생성까지 됨
- 다음 할 것: 설치해서 실제 빌드 산출물로 확인. 그다음 5번(링크 에러 해독)이나 4번(전처리·어셈블리 열기)을 같은 확장에 붙이기. 1·2·3·7 번은 스크립트로 먼저 재 보기
- 막힌 것: 없음. 로더가 실행 중에 추가하는 경로(Code.exe 의 ffmpeg.dll 같은 경우)는 밖에서 볼 수 없어 "못 찾음" 으로 나옴 — README 에 적어 둠
